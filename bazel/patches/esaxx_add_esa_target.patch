Export the esa.hxx suffix-tree enumeration header, which the module's
BUILD file does not expose (it only provides :sais for sais.hxx).

--- BUILD.bazel
+++ BUILD.bazel
@@ -3,6 +3,13 @@
 load("@rules_cc//cc:defs.bzl", "cc_library")
 
 cc_library(
+    name = "esa",
+    hdrs = ["esa.hxx"],
+    includes = ["."],
+    deps = [":sais"],
+)
+
+cc_library(
     name = "sais",
     hdrs = ["sais.hxx"],
     includes = ["."],
